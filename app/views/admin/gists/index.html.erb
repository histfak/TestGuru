<div class="container">
  <h1 class="text-center"><%= t('.header') %></h1>
  <div class="row">
    <table class="table table-hover table-condensed">
      <tr>
        <th><%= t('.question') %></th>
        <th><%= t('.url') %></th>
        <th><%= t('.user') %></th>
      </tr>

      <%= render @gists %>
    </table>
  </div>
</div>
